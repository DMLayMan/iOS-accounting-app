# 余记 · 产品上下文

平台：iOS / iPhone 竖屏，SwiftUI + UIKit；首阶段个人长期使用，保留 App Store 发布能力。

用户已经明确：简洁、美观、操作顺手；每笔账只归属一个二级分类，备注只是信息记录，不参与筛选；一级一行、二级最多两行，行内从左到右后换行，超出横划；长按排序；二级区域常驻新增入口。此文件整理已确认需求，不是新一轮假设或用户访谈。

## 主要任务

- 高频：打开 → 输入金额 → 选择二级分类 → 保存。分类名与图标可管理。
- 被打断：关闭新建自动留本地草稿；草稿不影响余额和统计；编辑旧账不能覆盖其他新建草稿。
- 分析：确认时期和收支口径 → 点击饼图或分类排行 → 二级筛选 → 同页流水；只在打开单笔记录时进入详情。
- 账本：查看当前账本从首笔至今天的累计收支、账户余额和记录概况。切换与新建在账本名称旁明确展示；创建成功后直接使用新账本。
- 查账：流水默认本月，顶部固定显示时期和笔数；点紧凑年月入口选择月、年、全部或自定日期范围，与统计共享期间，再叠加分类/收支类型筛选。进入单笔后返回保留上下文。
- 时间入口统一使用 iCost 统计页参考结构：流水与统计均为上方月/年/全部/范围，下方居中年月、两侧前后按钮。完整时间栏及日期面板由同一组件负责，两个页面不各自增加周期 Tab 或样式分支。两页共享期间；自然月/年连续切换，不跳空月；全部、自定不显示前后按钮。流水笔数、收支与筛选条件独立放在时间栏下方。
- 预算：每个账本可设置独立的月额度、年额度，创建时可填、之后可调整。额度按周期重置，不滚存；修改从本月/本年开始，过去周期的额度保留。留空不设预算，0 为零支出计划。统计展示该账本整期额度、已用和剩余；输入账单时预览保存后的剩余或超额。
- 分类预算：预算管理可添加 0 到多条月/年规则，范围为一级分类（含全部下级）或单个二级支出分类。同一范围每周期一条，父子预算独立提醒、不相加。统计默认只展示超额最多的两条，其余同页展开；记账只展示所选分类相关规则，支持同样的折叠。删除规则从本月/本年起停用，历史周期保留。
- 连续记账：系统底部 Tab 按“账本、流水、记账、统计、我的”排列，正中主题色加号打开录入，保留发起页的选中状态与导航位置。保存或取消后留在原 Tab；草稿不占预算，超预算不阻止保存。
- 外观：我的 → 外观与反馈，可选六种主题或自定义颜色，支持跟随系统/浅色/深色。主题调整按钮、导航、分类选中态；支出暖橙、收入青绿、退款蓝色、转账中性，保留类型和正负号。设置保存在本地账本数据中，不依赖网络。
- 维护：账户、分类、账本、备份。错误就地可改、保存失败保留输入、破坏性替换说明范围。

## 边界

本地数据为当前实现；iCloud 尚未开放。无自建服务端、云端图标、埋点上传或在线分类推荐。旧标签仅在历史附加信息与备份中保留，不在记账入口引入第二套概念。金额存整数分，转账不参与收支统计，退款按退款发生日计入原分类。真机签名此前未配置，本轮使用模拟器验收。

iCloud 同账号同步与多账号共享方案见 docs/icloud-sharing-plan-2026-09-10.md，尚未实现或验证云端连接。拟采用系统 iCloud 身份与 CKShare 私有邀请，不另建余记账号及组队服务。

## 效率目标与验证

目标是让用户少离开当前任务、少记忆上下文。可自动化验收的是：分类创建一步保存并立即选中；分析筛选不出现新 sheet、不改变分析页；单笔返回保留筛选；失败不退出；重启后数据一致。人类长期使用速度和美感满意度不能由测试分数冒充。

## 当前文档与恢复保障（2026-09-11）

[完整 PRD](docs/PRD.md)、[UI 复刻清单](docs/UI-SPEC.md)、[架构](docs/ARCHITECTURE.md)为最新基线。读取失败保持原文件与路径，暂停写入并显示恢复入口；重新读取或备份恢复成功后才开放账本。退款可删除并释放可退额度；永久删除保护原支出关系，空账本删除先说明目录/预算/草稿范围。当前验收以[回归记录](docs/regression-2026-09-11.md)为准。
