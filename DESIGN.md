# 余记 · 界面与动线规范

平台：ios。模式：Operate。保留白底、系统字体和 SF Symbols；默认青竹主题。主题色表达操作，收支用独立语义色。不是营销页，不引入大面积渐变、装饰玻璃、炫技动效、卡片套卡片。

## 信息与视觉层级

1. 当前任务和金额优先；分类、日期、账户次之；规则说明就近且短。
2. 主操作用主题色实心；普通导航和筛选用文本/系统控件；选中项同时有形状或勾选标识。主题色不改变收支语义，不把所有正文染色。
3. 正文使用系统语义字号；金额等宽数字且可适配；深色使用对应前景和背景，禁止浅绿实心按钮上白字。
4. 页面内边距 20–24 pt，组内间距 8–12 pt，组间距 18–24 pt；触控区域至少 44 pt。
5. 根页面采用系统 TabView，顺序为“账本、流水、记账、统计、我的”。正中记账是动作入口：主题色圆角底，浅色白色加号、深色黑色加号，适度突出；打开 sheet 而不选择空白 Tab，保存/取消恢复发起页。删除右下悬浮按钮和额外 76 pt 留白。系统 Tab 可见的页面均可使用记账；编辑 sheet 覆盖 Tab，避免键盘叠层。

## 色彩规则

- 支出暖橙（浅色 #A65332 / 深色 #E8AB8A）、收入青绿（#237A68 / #95CEB8）、退款蓝色（#516FA1 / #A6BDE5）；转账、余额和结余使用系统中性色。金额正负号与类型文字继续存在；不只靠颜色识别。
- 净支出大于零用支出色，小于零用退款色，零用中性色。收入与支出颜色在记账、流水、详情、账本及统计保持一致。同比/环比增减不做红绿好坏判断。
- 分类饼图用不同分类色表达构成，金额中心仍遵循收支语义；图例保留分类名、数值、百分比。主题与分类图表颜色各有用途。
- 六种主题：青竹、海蓝、暮紫、烟玫、琥珀、石墨；自定义使用系统选色器。浅色深色分别调整明暗，主题文字在基础/分组/选中背景及实心操作上的目标对比度至少 4.5:1。透明材质与系统禁用控件不冒充全页面 WCAG 验收。
- UIKit 分类组件显式接收与 SwiftUI 相同的主题色，禁止再写固定绿色。禁用保存按钮用中性底和次级文字；错误/超预算保留红色与说明。

## 页面规则

- 账本：顶部标题固定为“账本”，页内显示当前账本名称及“切换账本 / 新建账本”。主内容依次为累计收支、账户余额、记录概况，不重复排列最近流水。累计结余不含期初余额和转账；账户余额包含期初金额及已归档账户历史余额，均截至今天。
- 新建账本：独立短表单，70% sheet 可展开，填写名称后“创建并使用”。成功后自动切换；保存失败留在原表单且不改变当前账本，未填写时禁止创建。
- 预算：新建账本表单增加每月/每年两项可选金额。已有账本从累计收支旁“预算”或统计预算区“管理”进入。统计用文字与细进度条展示整账本预算，不随分类下钻缩小分母；月/年额度独立。录入用金额下方两行轻提示，预览保存后额度，不叠确认弹窗、不限制超额保存。红色必须同时出现“超出”文案。
- 分类预算：在同一个预算管理表单中列出规则、添加或点击编辑；编辑在 sheet 内导航，避免层层叠 sheet。填写月/年、一级或二级范围、金额；规则暂存到表单，统一保存，取消不落盘。统计与记账默认最多两条分类超额提示，按超额金额降序，点“全部 N 项”在原位置展开所有规则及预算/已用金额。无超额只留一行状态与展开入口，不展开健康规则占屏。
- 流水与统计：按下述 iCost 结构呈现时间栏；点击年月打开共用面板选月/年/自定/全部。月份用直接选年加 12 个月网格，选中即应用，取消不提交，自定日期完整填写再应用。两页共享当前期间；切换账本回本月，保存交易保留期间。流水包含所选整期及未来记录；统计截至今天，并明确实际日期范围。纯未来期间统计显示说明，不能作为零支出结论。全部/自定显示范围汇总，不强套月年预算与同比。
- 记账：金额键盘与系统文字键盘互斥；新建关闭留草稿；编辑放弃在页内确认。
- 时间栏统一采用 iCost 统计页参考结构：流水和统计共用完整的 PeriodNavigation，上行系统分段选择月/年/全部/范围，下行年月居中、两端圆形方向按钮。分段、日期、按钮、内边距、日期面板及取消/应用逻辑全部由组件管理，不保留页面样式分支。流水的笔数与收支摘要在组件下方。使用当前主题和系统语义色；实际箭头命中区域 44 pt，最大字号日期可简写但保留完整无障碍年月名称。日期更改同步到两页；取消表单恢复实际期间及选中态。
- 新增/编辑分类：70% sheet，可展开；同一处展示图标预览、名称、一级归属、图标选择、保存。打开先看完整表单，输入文字时允许展开。选择图标后仍留在表单。保存成功后回到原账单并选中新分类。
- 统计：饼图与紧凑排行并列；分类选中联动下方二级筛选、汇总、流水。默认前四类可展开，不把全部排行塞在明细前。图表占比按毛支出/收入，总分母不随单个分类选择偷偷变化。退款单列。
- 同环比：页内展开本期/基期，明确所查看的日期范围；切换同步图表和流水。净支出变化与收入分布不可混称。
- 设置：遵循系统分组列表，不为了风格重造开关；文案不暴露实现术语，不展示可点击但无效果的功能。
- 数据：生成文件与存储到外部位置是两件事，状态须准确；恢复先预览范围、再确认替换，保存替换前恢复点。

## 交付门槛

按 PRODUCT.md 中的动线先判断新增弹层是否必要。每次 UI 改动至少检查默认 iPhone、小 iPhone、深色、最大无障碍字号；查看原生截图，测试页面进入/退出、空状态、异常金额、选中态、键盘和失败留存。不得以 HTML 示意、编译成功或测试存在代替原生效果验证。性能/触觉/VoiceOver 未实测时明示限制。

## 当前复刻入口与组件边界（2026-09-11）

完整场景见 [PRD](docs/PRD.md)，每页顺序、尺寸与状态见 [UI 复刻清单](docs/UI-SPEC.md)，原生图集见 [当前截图](docs/assets/current/index.html)。历史日期文档中的浮动按钮、滚轮和标签页面不再生效。

流水行、金额/日期格式、分类图标已归入 `Components`。预算规则展示、日期完整头部、表单退出保护、主题与语义色保持一个实现入口；分类拖动与系统选色器只负责原生交互，落盘仍走统一状态层。新增组件必须说明输入、输出及业务语义，避免复制现有页面代码。

损坏数据恢复页采用系统导航列表：读取失败说明、重新读取、选择备份恢复；不显示能写入空账本的 Tab。数据页禁用导出，恢复前先保留原始文件。空账本删除、永久删除与全库恢复使用明确确认；新建/编辑普通表单继续使用页内退出保护。
