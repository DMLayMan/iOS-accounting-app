# 页面契约：填写页面名

目的：让另一位实现者不依赖作者口头补充也能还原页面。不是所有字段都适用于每页，可删去无关项。

## 任务与结构

- 用户在此页完成什么：
- 从哪里进入，首次/再次进入默认定位：
- 完成与取消分别回到哪里：
- 自上而下的信息顺序：
- 固定区域、滚动区域、主操作的位置：
- 视觉参考及对应版本：

## 组件契约

| 组件 | 输入 | 事件输出 | 状态拥有者 | 是否写入/由谁写入 |
| --- | --- | --- | --- | --- |
| 填写实际组件 | | | | |

共用组件的同名语义、取消、日期与选中态是否在所有调用方一致？需要不同的地方写明原因，不用页面名驱动隐藏样式分支。

## 交互与状态

| 触发 | 当前状态 | 下一状态与反馈 | 保留/重置的数据和上下文 |
| --- | --- | --- | --- |
| 主要动作成功 | | | |
| 输入与键盘切换 | | | |
| 取消/下滑/返回 | | | |
| 无数据/无结果 | | | |
| 保存失败/权限拒绝 | | | |

手势的作用区域、方向、取消、非手势替代：

新建与编辑的 dirty 判定、草稿所有权、关闭策略：

## 呈现规则

- 字号/间距/触控区域引用的设计 token：
- 强调色、业务语义色、图表色的用途：
- 小屏、大字号、深色、长文案、键盘时的布局变化：
- 有图表时的指标/分母/日期/联动范围：
- 有动效时的目的、可中断行为与减少动态效果方案：
- 无障碍名称、状态和值：

## 证据

当前原生截图与路径记录：

对应测试或人工走查：

仍是设计假设/尚未实测的状态：
